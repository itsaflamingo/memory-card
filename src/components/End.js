import React, { useState } from "react"

export default function End() {
    return (
        <div id='result' className="popup">
            <div id='message'>
                {/* if win, else if lose */}
            </div>
            <button id='play-again'>Play Again</button>
        </div>
    )
}